import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lib-babygrowth-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
