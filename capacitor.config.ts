import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lib-tinybaby-angular-ionic',
  webDir: 'www'
};

export default config;
